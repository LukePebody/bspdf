from .common import cbblock as decode
ENGLISH_NAME = "Color Block"
RENDERER = "cbblock"
RULES = (
    "Divide the grid into blocks, each containing exactly two of the outlined regions.",
    "No block is rectangular, and adjacent blocks cannot have the same shape up to rotation or reflection.",
)
