from .common import number16 as decode
ENGLISH_NAME = "Shikaku"
RENDERER = "nurikabe"
RULES = (
    "Divide the entire grid into rectangles.",
    "Each rectangle contains exactly one clue, whose number equals the rectangle's area in cells.",
)
